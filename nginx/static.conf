server {
    listen 80;
    server_name example.com;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ $uri/index.html =404;
    }

    # Hide metadata files
    location ~ /_meta\.json$ {
        deny all;
    }
}
